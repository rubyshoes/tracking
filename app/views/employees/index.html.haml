- provide(:title, 'All employees')
%h2 All Employees
= will_paginate
%ul.employees
  = render @employees # the @employees is the object returned by will_paginate and so Rails will iterate through the object. Each of the @employees is of a class user so Rails knows to look for the partial (_employee.html.haml) 
%br/
= will_paginate
