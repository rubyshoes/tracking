<% if @code.errors.any? %>
  <div id="error_explanation">
    <h5><%= pluralize(@code.errors.count, "error") %>
      prohibited this code from being saved:</h5>
    <p>There were problems with the following fields:</p>
    <ul>
    <% @code.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
