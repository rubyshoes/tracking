<% if @codeline.errors.any? %>
  <div id="error_explanation">
    <h5><%= pluralize(@codeline.errors.count, "error") %>
      prohibited this codeline from being saved:</h5>
    <p>There were problems with the following fields:</p>
    <ul>
    <% @codeline.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
