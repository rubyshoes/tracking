- provide(:title, 'Add Contract')
%h2 New Contract
= form_for(@contract) do |contract|
  =render 'shared/contract_error_messages', object: contract.object
  =render 'fields', contract:  contract
  .actions
    = contract.submit "Save", class: 'save strong round'
